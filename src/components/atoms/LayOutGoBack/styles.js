import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  goback: {
    position: 'absolute',
    top: '6.897%',
    left: '9.533%',
  },
  screenSize: {
    height: '100%',
    width: '100%',
    position: 'relative',
  },
});

export default styles;
