import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '80.54%',
    alignSelf: 'center',
    marginTop: 39.54,
    maxHeight: '60%',
    height: '60%',
  },
});

export default styles;
